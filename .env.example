# Set a token to require authentication for API and receipt downloads.
# The web page also supports this same token.
FLUX_API_TOKEN=

# Finished job directories expire after 7 days and are also capped at 100.
FLUX_RETENTION_HOURS=168
FLUX_MAX_SAVED_JOBS=100
