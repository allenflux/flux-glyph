# 统一字体模型的容量与字重修复

当前仓库发布包为实验版 `r22-unified-font-retention-v1-preview`。微米黑恢复轮已完成本地 MPS 固定 6,000 步，原 46 项 retention 条件、18 项重复开发回归、单 ONNX 全量字体／字号一致性和动态批次检查均已通过，本地 Docker 下载与独立 CLI 交付验证也已完成。CAL 和开发集都已被复用，不是盲测；通用稳定验收未通过，TEST 未执行，模型仍为实验版。实际服务版本以 `/api/health` 与 `/api/models/font` 为准。本阶段继续保留 24 个具名字体和未知类；模型输入仍只有图像块。

## 已完成的损失实验

`run-confidence-floor-v1` 实际完成 3,000 步，只评估最终检查点。它将未知类监督改为置信度下限，并使用单向真实类别置信度保护，允许正确结果超过原教师的置信度。最终只通过 40／46 项条件，没有导出或部署。

清晰 PingFang 校准锚点正确命名为 348／362，但整体未知字体不命名率降至 82.75%，低于原要求 84.63%；Android 命名精度为 89.10%，低于 90.25%；霞鹜文楷正确覆盖为 396／868，低于最低 449。改善部分 iOS 样本没有抵消这些退化。

## 训练数据诊断

对相同的三份 TRAIN 数据做了 R21、b08 初始化候选和本轮权重的比较。共 137,230 个图块，包含 73,966 个相关视图、20,763 个原生区域。以下是训练图块的 argmax 命中率，不是未见样本准确率；同一区域的不同视图相互关联。

| TRAIN 真值 | R21 | b08 | 置信度下限实验 |
|---|---:|---:|---:|
| 霞鹜文楷 | 86.24% | 89.18% | 86.21% |
| 文泉驿微米黑 | 70.60% | 77.18% | 75.65% |

本轮对新增未知字体的训练图块仍存在明显混淆：朱雀仿宋的未知类命中率为 36.52%，文泉驿正黑为 45.09%。原始 TRAIN 中，R21 的未知类命中率为 95.53%，b08 为 83.16%。这些结果支持继续检查特征学习和旧知识保留，不能证明模型容量是唯一原因。

逐图块核对全部 137,230 个 TRAIN 输入及其文件哈希，没有发现完全相同的 float32 图块跨真实类别出现；这只能排除完全相同输入的标签冲突，不能排除近似字形的歧义。采样回放另显示，配对补充集只含 Regular 文楷，使 6,000 步内文楷采样为 Regular 8,000 次、Light／Medium 各 2,000 次。原始数据中的三个字重本来接近均衡。这是后续补充配对样本时需要处理的覆盖问题。

## 加宽网络实验

新模型保持一个编码器，将隐藏通道从 `32/48/64/64/192/128` 增加到 `64/96/128/128/384/256`，GroupNorm 组数从 8 增至 16。通过复制完整通道组、拆分后续连接权重，将 b08 的原始输出函数转移到更宽的网络，再进行训练。方法参考 [Net2Net](https://arxiv.org/abs/1511.05641)，GroupNorm 的整组复制是本实现的适配。

训练时，真实标签为已知字体的图块使用冻结 b08 教师，真实未知类使用冻结 R21 教师。仅在所选教师第一预测等于真实标签时加入分布蒸馏；教师输出与图像块逐一对应。这个选择只发生在离线训练，依据真实标签，最终推理没有教师模型、平台选择或评分合并。

本次固定为本地 MPS、6,000 步、最后一步评估，不搜索中间检查点。继续使用相同三份 TRAIN 数据及采样比例，已知类平滑交叉熵、未知类 0.5 置信度下限、0.2 倍字号损失和 2 倍教师 KL；推理温度、字体评分门槛、图块一致性和字号门槛均不变。

启动前 54 项真实 Torch CPU 测试通过，包括字体与非零字号输出的转移一致性、精确图块顺序、教师选择、损失数值、梯度以及全部四个参数组更新。正式训练前，384 个固定 TRAIN 图块的 MPS 转移检查也已通过：字体 logits 最大绝对差为 `4.3869e-5`，字号 log-ratio 最大绝对差为 `3.5763e-7`，源权重和初始加宽权重均未在检查中改变。参数由 365,034 增至 1,450,938；一致性和代码测试通过不代表字体识别验收通过。

最终校准结果为：整体命名精度 95.22%，iOS 命名精度 99.08%；原生 PingFang 锚点正确命名 349／362，误命名 0。仍有以下五项未达标，未继续读取开发回归图片或导出模型：

| 校准条件 | 实际结果 | 原要求 |
|---|---:|---:|
| 整体未知字体不命名率 | 84.36% | ≥84.63% |
| Android 命名精度 | 90.07% | ≥90.25% |
| Android 已知正确覆盖 | 51.10% | ≥51.13% |
| 文楷正确命名 | 369／868 | ≥449／868 |
| 微米黑正确命名 | 183／868 | ≥187／868 |

本轮结果记录在本地 `artifacts/unified-font-v3/WIDE_TRIAL_RESULT.json`，SHA-256 为 `320b8fc914ef2d92652936bad4afd0dcd6decb2db8790ff2a3c7933a28b5d40c`；权重 SHA-256 为 `9646065c4d1629287a22e3ffb4088472e588b5148bc8af649713e32779533813`。扩大容量并保留旧知识改善了部分指标，仍未解决近似字体的覆盖问题。

## 后续字重补充

使用独立的本地 Android 模拟器和新目录完成了 40 页 TRAIN 布局的采集。其中 Light／Medium 各 120 个原生区域，分别包含 100 个汉字、10 个英文、10 个数字区域，另保留 240 个微米黑配对布局供原生采集验证。沿用已核对的文本、字号、颜色、框和背景，逐字校验字体文件、PostScript 名称和 glyph ID。480 个原生区域已生成 1,920 个相关视图、3,215 个图块，无视图被拒绝。

实际合并保留旧 Regular／微米黑样本，只新增 Light／Medium 文楷，排除重复的微米黑。合并后共 720 个原生区域、2,880 个相关视图、4,820 个图块；每个图块均重新计算冻结 b08 教师输出。原始 TRAIN、未知补充和本次具名字体补充合计为 21,003 个原生区域、74,926 个相关视图、138,830 个图块。历史教师缓存保留独立来源记录，没有拿旧的 3,220 个具名字体输出冒充新的 4,820 个输出。

字重均衡轮 `run-wide-face-balanced-v1` 延续相同的加宽初始化、损失、6,000 步最终检查点和推理规则。97 项原生 Torch CPU 检查通过；对实际 TRAIN 元数据的完整采样回放确认，配对文楷三个字重各采样 2,000 次，连同原始 TRAIN 后各为 4,000 次。核查同时修复了采样器不接受真实 `WenQuanYiMicroHei` 字体名称的问题。教师资格依据同一图块的缓存输出是否命中真实标签计算，预检查不是识别率验收。

校准集已经被前序开发反复使用，结果不能当作盲测或真实手机准确率。只有全部 46 项条件通过，才会继续 ONNX 一致性验证和原 18 项开发回归；本阶段尚未读取 TEST。用户截图没有字体真值，没有用作训练标签。

## 字重均衡轮的结果与后续训练

`run-wide-face-balanced-v1` 已完成实际 6,000 步，本地 MPS 训练耗时 1,840.1 秒（不含数据检查和最终评估）。43／46 项通过，整体未知字体不命名率升至 84.79%，微米黑正确命名 189／868，均达到原要求。原生 PingFang 锚点仍为 349／362、误命名 0。

| 未通过的校准条件 | 实际结果 | 原要求 |
|---|---:|---:|
| Android 命名精度 | 90.2065% | ≥90.2480% |
| Android 已知正确覆盖 | 50.9990% | ≥51.1305% |
| 文楷正确命名 | 377／868 | ≥449／868 |

模型未继续导出或读取开发回归图片。结果记录 `artifacts/unified-font-v3/FACE_BALANCED_TRIAL_RESULT.json` 的 SHA-256 为 `1b264b47bbb4ff9f99dfddb3e8d9334c19b1d73fcacfafdbfb3da07b350a2392`；权重 SHA-256 为 `b64e7f5bfe46b2a8e05cd7e1c1a75c4d987a1b0b0313a807db05fc5f9840d22c`。

后续训练借鉴 [ArcFace 的类别角度间距方法](https://arxiv.org/abs/1801.07698)，在真实已知字体的 TRAIN 样本上加入辅助损失。辅助项复用现有特征与分类权重，推理继续输出原始字体 logits 和字号，不增加模型或推理评分规则。论文的效果来自人脸识别，能否改善本项目的字体混淆仍需实测。

`run-wide-angular-margin-v1` 已通过 12 个实际 TRAIN 批次、1,152 个图块的训练前检查。辅助项系数为 0.10、角度间距 0.10 弧度、归一化尺度 16；辅助梯度与原损失梯度的总体范数比为 0.1827，单批最大为 0.2582。未知行的辅助特征梯度严格为零，原始字体与非零字号输出逐位一致，检查未更新权重，也未读取校准、开发或测试图片。41 项原生 Torch 代码验证通过。

新方案在第一步优化前已冻结，计划 SHA-256 为 `d5cb8ecc3d60ae4b940bc5eee9da77783a610b425b8defa1356cb4091342cf2a`。随后启动本地 MPS 的固定 6,000 步训练，只评估最终检查点。数据、字重采样、初始化、原损失、教师与推理门槛沿用上一轮；训练启动和代码验证均不代表识别问题已经修复。

## 类别间距轮结果与教师约束调整

类别间距轮已完成实际 6,000 步，本地 MPS 优化耗时 1,874.1 秒，通过 45／46 项条件。Android 命名精度 90.3500%、已知正确覆盖 51.2759%，均达到原要求；整体未知字体不命名率 84.8687%。原生 PingFang 锚点正确命名 350／362、误命名 0，SF Pro 为 127／129、误命名 0，Helvetica 为 52／52。唯一未通过的条件是文楷正确命名 380／868，低于最低 449／868；本轮未导出或读取开发回归图片。

结果记录 `artifacts/unified-font-v3/ANGULAR_MARGIN_TRIAL_RESULT.json` 的 SHA-256 为 `a9a8753827db8313008f16c2073c5d7c97df3c7765b0c1536cbe683bba632a97`；权重 SHA-256 为 `a15a94d42b3d5b5bb1170be836f0018f20b56eb3561f812ae558ddf2086c8ee4`。

实际 TRAIN 元数据与教师缓存的完整回放显示：9,185 次教师命中的配对采样中，4,688 次真实类别概率低于 0.7。已知字体的完整分布蒸馏会惩罚偏离这些较弱分布，包括向正确方向提高置信度。这是继续调整训练损失的依据；简化的逐行损失分析不等于模型准确率预测。

后续 `run-wide-named-relearning-v1` 仅替换教师约束：已知真实类别使用单向真实类别置信度下限，允许超过旧教师；未知真实类别继续使用完整 R21 分布 KL。两部分仍按总资格行数归一化，整体权重仍为 2。角度间距辅助项、字体交叉熵、字号损失、样本与采样、初始化、6,000 步最终检查点和推理规则均不变。

12 个实际 TRAIN 批次的检查通过：未知行的教师梯度与原完整 KL 逐位一致；354 个真实类别概率已超过教师的已知行不再产生教师约束梯度；61 个教师猜错的行也不产生该梯度。全部四个参数组收到有限且非零的总梯度，字体和非零字号输出逐位一致，权重未在检查中更新。这些 TRAIN 已用于此前开发，新增 Light／Medium 配对图块并非 b08 初始化模型的优化样本，不能将检查称为盲测。

该轮计划在首个优化步骤前冻结，SHA-256 为 `d85be1cda0cf792a79cc8095e7bdfe35f18b791dd8586c66d76e40e294b3043e`。随后已完成本地 MPS 的 6,000 步训练，优化耗时 1,892.2 秒。原有 46 项校准与 18 项开发条件保留。

文楷正确覆盖提高到 435／868，微米黑提高到 237／868；但整体未知字体误命名从 386 增至 454，未知字体不命名率降至 82.2031%。Android 命名精度降至 89.0459%，iOS 降至 98.9167%；最终仅通过 40／46 项条件。文楷仍未达到最低 449／868，覆盖提升没有抵消整体误判增加。该轮未导出或读取开发回归图片。

结果记录 `artifacts/unified-font-v3/NAMED_RELEARNING_TRIAL_RESULT.json` 的 SHA-256 为 `2b0eae1d61c4cc1e3e1e53da3a2fda4d4f95803e49806f789f828e8b38d31154`；权重 SHA-256 为 `b047b009f1bfa2458e71a92d29ebf8ec7856dddc4dc2c39ed59549ba8643cc5a`。

逐行比较已有校准结果：386 个原有未知字体误命名全部保留，另外增加 68 个，其中 65 个被命名为文楷、微米黑之外的类别。文楷正确首选类别仅从 704 增至 705；覆盖增长主要来自原先低分的正确候选越过固定门槛。该诊断重复使用旧校准结果，不是新的独立评估。

## 文楷监督权重恢复实验

下一轮以通过 45／46 项的类别间距辅助轮为基准，仅将真实 TRAIN 文楷样本的平滑交叉熵权重从 1 调整为 2。这个幅度与已有原生 iOS 系统字体重点样本的监督权重相同；所有教师判断正确的行仍使用原来的完整 KL 约束，字号损失、类别间距辅助损失、采样序列和识别阈值均保持原值。这是训练中的类别权重，不是识别时按平台选择字体或修改分数。

12 个实际 TRAIN 批次检查已通过：24 个文楷样本的额外梯度恰好来自新增的一份平滑交叉熵，其他字体行的分类梯度逐位不变，教师 KL、字号与类别间距辅助项的值和梯度也逐位不变。四个参数组均收到有限且非零的总梯度，检查未更新权重。方案 SHA-256 为 `15fa1ebf5d554a62d31c114142208f3f23e61534e773e910a2febe769bc4550a`，已在本机 MPS 完成 6,000 步训练，优化耗时 1,905.2 秒。文楷正确命名 454／868，超过原最低 449／868；微米黑正确命名 182／868，低于最低 187／868，成为唯一未通过的条件。整体未知字体误命名仍为 386；整体命名精度 95.3731%、Android 命名精度 90.5301%，其余 45 项均通过。该轮未导出、未读取开发回归图片或 TEST。

结果记录 `artifacts/unified-font-v3/WENKAI_RECOVERY_TRIAL_RESULT.json` 的 SHA-256 为 `2fd81bb22e107b6a61eabf689c045f93ddf234d6c2a3358b2dfb3435030e7fef`。后续保持这项文楷改动，只给真实 TRAIN 微米黑样本增加四分之一份平滑交叉熵，使其权重从 1 到 1.25。12 个实际 TRAIN 批次的梯度检查通过：新增项只改变预定微米黑行的分类梯度，文楷、其他类别、完整教师 KL、字号和类别间距辅助项在同样权重下保持原值及梯度。

微米黑恢复轮方案 SHA-256 为 `d8a6f96275a77f6909a43df569be585c71481e6af6519325af346f46066bad35`。本轮已在本机 MPS 完成固定 6,000 步训练，原 46 项 retention 条件全部通过。文楷正确命名为 456／868，微米黑为 209／868；原生 PingFang 锚点为 350／362、SF Pro 为 127／129、Helvetica 为 52／52，三组误命名均为 0。shared PingFang、SF Pro、Helvetica 正确命名分别为 1,413、697、434，也都超过计划参考线。

这些参考线需要和原始 R21 数字分开解释。文楷覆盖率为 52.5346%，仍比 raw R21 低 2.1890 个百分点，但高于计划允许的下限 0.8111 个百分点；微米黑覆盖率为 24.0783%，比 raw R21 低 0.4608 个百分点，但高于允许下限 2.5392 个百分点。shared PingFang 的计划下限 1,372 来自计划注明的更强 R20 参考，而 raw R21 为 1,217；不能把所有计划参考线都直接写成 raw R21。整体、iOS、Android 的命名精度和已知覆盖相对 raw R21 均有提高，但 iOS 未知误命名由 40 增至 49，仍是限制。

本轮复用了已经被多轮开发使用的 CAL，结果不是盲测。`promotion_allowed` 表示 46 项 retention 通过；通用 `passed` 和 `calibration_passed` 仍为 false，不能表述为通用稳定验收通过。试验结果记录 `artifacts/unified-font-v3/MICRO_RECOVERY_TRIAL_RESULT.json` 的 SHA-256 为 `4541e7ed83d28633727cb98719edf612e7c63186e5da8c7962b5656ae18fad05`，权重 SHA-256 为 `53d73321c109825014fca96435f08a8c7a01ec6fe2abefa727855e1500d94c42`。

导出的单 ONNX SHA-256 为 `a8aadf20ecf175b824618142b8bb3dafb9148c099046f83e161150c90b3d655b`，已经通过全量字体／字号一致性和批次 1、7、32、128 的动态批次检查。随后重复执行的开发回归 18／18 项全部通过，报告 `artifacts/unified-font-v3/run-wide-micro-recovery-v1/DEVELOPMENT_REGRESSION.json` 的 SHA-256 为 `af6ad00930131ecdf53083a5d3ce836fb4904de15c61f1f072c8bfc7b7f77c22`。相对 R21 开发基线，整体已知正确覆盖由 51.6571% 提高到 53.6966%，但命名精度由 96.4881% 回退到 96.3408%，未知不命名率由 98.3607% 回退到 96.9945%；这些回退都在预先规定的允许范围内。Android 也呈现覆盖提高、精度和未知拒绝回退的取舍，iOS 覆盖提高且精度与未知拒绝保持原水平，因此不能概括为所有指标全面提高。

开发回归使用的是同一组受控区域和相关视图，历史初始化可能见过其中图片，不是盲测，也没有用于选择训练检查点。报告继续标记 `stable_validation_passed=false` 和 `test_passed=false`。

实验版发布包 `r22-unified-font-retention-v1-preview` 已完成本地 Docker 交付验证。验证使用两张固定 TRAIN 页面，分别核对 API、直接调用和干净独立 CLI，共 6 组字体、字号与颜色结果一致；下载包内只含一个字体 ONNX。本地交付报告 `artifacts/unified-font-v3/delivery-micro-recovery-v1/REPORT.json` 的 SHA-256 为 `e5b27e69eb3d7ba1350c6cb9e96469878c2d7c4d65446639ce392b469334b771`。这些固定 TRAIN 页面只验证打包和调用链一致性，不是新的准确率评估。实际服务版本以 `/api/health` 与 `/api/models/font` 为准。

另一个按真实类别／其他类别分解的教师损失通过了独立数值检查，但没有加入本轮训练；其思路参考 [Decoupled Knowledge Distillation](https://arxiv.org/abs/2203.08679)，本项目的单向变体尚无字体识别效果证据。
