# Local iOS region model: text detection + font/size CNN, without text recognition.
# docker compose -f compose.yaml -f compose.ios.yaml up -d --build
services:
  api:
    image: flux-glyph:ios-local
    volumes:
      - ./artifacts/ios-region-font-v1/bundle-stable:/app/models:ro
